// function for showing popup message after user registers

function showMessage(message)
{
    // check message exists or not

    if(message != "null" && message != "")  
    {
        alert(message);
    }
}